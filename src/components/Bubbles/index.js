import { Container } from './styles';

export const Bubbles = ({ gc, b1, t1, l1, r1, b2, t2, l2, r2 }) => {
  return (
    <Container
      gc={gc}
      b1={b1}
      t1={t1}
      l1={l1}
      r1={r1}
      b2={b2}
      t2={t2}
      l2={l2}
      r2={r2}
    />
  );
};
